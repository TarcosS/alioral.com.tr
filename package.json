{
  "name": "alioral.com.tr",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preinstall": "npm install -g pm2 --save && npm start",
    "start": "cd Client && pm2 start --name alioral/client npm -- start && cd .. && cd Server && pm2 start --name alioral/server npx -- npm start",
    "restart": "pm2 delete all && pm2 flush && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
